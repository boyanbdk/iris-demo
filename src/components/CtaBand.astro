<section class="cta-band" aria-label="Покана от АвтоСилас">
  <div class="wrap cta-band__inner reveal">
    <h2>Искате такъв сайт за вашата клиника?</h2>
    <p>Това е измислен пример. АвтоСилас изгражда реалната версия според вашия екип, услуги и начин за записване.</p>
    <a class="btn btn--light" href="https://autosilas.com/offers/">Вижте пакетите на АвтоСилас</a>
  </div>
</section>

<style>
  .cta-band {
    background: var(--ink);
    color: #f2f1ec;
    padding-block: clamp(4rem, 8vw, 6.5rem);
  }
  .cta-band__inner {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.2rem;
  }
  .cta-band h2 {
    color: #f2f1ec;
    font-size: clamp(2.1rem, 4.6vw, 3.4rem);
  }
  .cta-band p {
    color: rgba(242, 241, 236, 0.72);
    max-width: 44ch;
  }
  .btn--light {
    background: #f2f1ec;
    color: var(--ink);
    font-size: 1.1rem;
    padding: 1rem 2rem;
  }
  .btn--light:hover {
    background: #fff;
  }
</style>
